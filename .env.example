OLLAMA_BASE_URL=http://localhost:11434/v1  # Ollama local inference (Phase 1 local tests)
TOGETHER_API_KEY=                          # Together.ai cloud inference (Phase 1 full run onwards)
JUDGE_MODEL=llama3.1:8b                    # scoring judge; set to meta-llama/Llama-3.1-8B-Instruct-Turbo for Together.ai
REPHRASE_MODEL=llama3.1:8b                 # P3 rephrasing model; same override pattern as JUDGE_MODEL
